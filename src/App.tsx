import { Header } from './components/Header';

function App() {
  return (
    <div className="app">
      <Header
        text="Feedback UI"
        bgColor="rgba(0, 0, 0, 0.4)"
        textColor="#ff6a95"
      />
    </div>
  );
}

export default App;
